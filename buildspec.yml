version: 0.2

phases:
  pre_build:
    commands:
      - echo Installing source NPM dependencies...
      - npm install
  build:
    commands:
      - echo Build started on `date`
      - npm run build
  post_build:
    commands:
      - sls deploy --app spa
      - sls syncToS3 --app spa
      - sls invalidateCache --app spa
artifacts:
  files:
    - '**/*'
  base-directory: build